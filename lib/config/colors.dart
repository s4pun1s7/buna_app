// Custom color constants (if not already in theme)
import 'package:flutter/material.dart';

class AppColors {
  static const Color bunaBlue = Color(0xFF0052CC);
  static const Color bunaPink = Color(0xFFFF8EB4);
  // Add more custom colors as needed
} 