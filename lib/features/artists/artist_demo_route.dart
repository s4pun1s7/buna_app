import 'package:flutter/material.dart';
import '../../widgets/artist_demo_screen.dart';

/// Temporary route for demoing the detailed sample artist card.
class ArtistDemoRoute extends StatelessWidget {
  const ArtistDemoRoute({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const ArtistDemoScreen();
  }
}
